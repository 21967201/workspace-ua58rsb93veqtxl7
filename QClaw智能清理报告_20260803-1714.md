# QClaw 智能清理报告（周度 + 月度深度）

- **执行时间**：2026-08-03（周一）17:14 Asia/Shanghai
- **任务来源**：cron `QClaw智能清理（周度+月度合并）`
- **模式**：全自动，无用户确认
- **月度深度分析**：已触发（当日 = 3，≤ 7）

---

## 一、目标

在不损失任何有效数据的前提下，回收临时文件/缓存/内存占用，并对 Skills、Agents、Workspace 做冷热分层体检，确认系统仍处于可持续运行状态。

---

## 二、周度清理执行结果

| 项目 | 结果 | 说明 |
|------|------|------|
| Workspace 临时文件 (*.tmp/*.temp/~*) | **0 个 / 0 MB** | 热区干净，无残留 |
| 系统临时目录 QClaw/OpenClaw 缓存 | **删除 29 项 / 33.77 MB** | `%TEMP%` 下过期缓存已清空 |
| 日志清理（>7天删除、>3天压缩） | **被安全策略拦截** | 见「四、异常与拦截」 |
| node_modules 缓存 | 与日志同批命令，一并被拦截 | 未执行 |
| 数据分级归档 `data-tiering.ps1 -Mode run` | **执行成功：无可归档文件** | 热区已是最优状态，SHA256 校验链无新增记录 |
| 内存清理（工作集回收 + GC） | **1.64 GB → 6.99 GB 可用** | 释放约 **5.35 GB**，触发条件满足（<4GB） |

内存 TOP5 进程（清理后）：node 246MB / AliRender 149MB / WorkBuddy 122MB / WeChatAppEx 99MB / __ima.copilot 98MB —— 均属正常水位，无异常泄漏。

---

## 三、月度深度分析（仅体检，未做破坏性删除）

### 3.1 Skills
- 总数：**135**（`D:\Users\Administrator\.qclaw\skills`）+ 139（skillhub-skills）
- 90 天未修改的「冷 Skill」：**0 个**
- 结论：数量虽超过 30 个阈值，但**全部在近 90 天内有更新痕迹**，不存在可安全回收的死技能。本轮**不执行 Skills 清理**，避免误删活跃能力。

### 3.2 Agents
- Agents 目录（`D:\QClawX\data\.qclaw\agents`）：**11 个**
- 结论：规模合理，无需清理。

### 3.3 Workspace
- 路径：`D:\QClawX\data\workspace-ua58rsb93veqtxl7`
- 文件数：**16,087**，总大小：**410.19 MB**
- 大文件 TOP 项目（均为必要依赖，不清理）：
  - `gbrain/src/assets/wasm/grammars/tree-sitter-sql.wasm` — 10.49 MB
  - `gbrain/node_modules/typescript/lib/typescript.js` — 8.69 MB
  - `gbrain/node_modules/@electric-sql/pglite/dist/pglite.wasm` — 8.34 MB
  - `memory/memory-index.json` — **8.14 MB**（记忆索引，持续膨胀，需关注）
  - `%BACKUP_DIR%/ua58rsb93veqtxl7/sessions/sessions.json` — **7.04 MB**（⚠️ 目录名是未展开的环境变量字面量，属历史脚本 bug 产物）

### 3.4 磁盘水位
| 盘符 | 已用 | 可用 |
|------|------|------|
| C: | 113 GB | **7 GB** ⚠️ |
| D: | 189.6 GB | 40.4 GB |

---

## 四、异常与拦截

**日志清理命令被系统安全策略拦截**：涉及批量 `Remove-Item`（>7 天日志删除 + node_modules 缓存目录递归删除），被判定为「可能清空重要数据」，要求交互式二次确认。本轮未绕过、未重试。

实际影响可忽略：独立统计显示 `D:\QClawX\data\logs` 下 7 天以上旧日志仅 **1 个文件 / 0 MB**，清不清都无关紧要。

---

## 五、结论与后续建议

1. **本轮清理有效收益**：临时缓存 33.77 MB + 内存 5.35 GB。系统状态健康。
2. **C 盘仅剩 7 GB 可用**，是当前唯一的真实风险点。按 AGENTS.md 规则 6，C 盘不应承载 QClaw 数据；建议下轮排查 C 盘占用大头（大概率是系统/微信/浏览器，而非 QClaw）。
3. **`%BACKUP_DIR%` 字面量目录**是历史脚本变量未展开留下的垃圾路径，占 7 MB，建议单独确认后清理并修复对应脚本。
4. **memory-index.json 已 8.14 MB**，建议纳入下次月度检查：评估是否需要分片或做索引瘦身。
5. Skills 135 个但零冷技能 —— 说明「最后修改时间」不是有效的活跃度指标（安装/同步会刷新时间戳）。下轮应改用 sessions 引用次数做判定，否则这条清理规则形同虚设。
