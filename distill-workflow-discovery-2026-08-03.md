# Distill 工作流发现报告 — 2026-08-03

> 任务：每 30 天自动扫描历史 session，识别重复模式并固化为可复用 skill
> 执行时间：2026-08-03 17:20 (Asia/Shanghai)
> 距上次运行（2026-07-20）：14 天

---

## 一、扫描统计

| 指标 | 数值 | 对比上次(07-20) |
|------|------|------|
| 扫描目录 | `C:\Users\Administrator\.qclaw\workspace\sessions` | 同 |
| 近 30 天 session 摘要文件 | **79**（.md 79 + store.json 79） | ↑ 65 → 79 |
| 达到最小重复阈值(≥3)的模式 | **8** | ↑ 3 → 8 |
| 新建 skill 提案 | **3** | ↑ 1 → 3 |
| 已有 skill 频次刷新 | **4** | ↑ 2 → 4 |
| 判定冗余不固化 | **2** | — |

> 说明：实际扫描目录为 `C:\Users\Administrator\.qclaw\workspace\sessions`（`~/.qclaw/workspace/sessions` 的真实落点）。任务描述中的其余三个候选路径（`~/.qclaw/sessions`、`D:\QClawX\data\.qclaw\sessions`、`D:\QClawX\data\sessions`）经 `Test-Path` 校验均不存在。

---

## 二、发现的高频模式（按任务标题语义聚类）

| # | 模式 | 近30天次数 | 置信度 | 已有 skill | 处置 |
|---|------|-----------|--------|-----------|------|
| 1 | GitHub 自动同步 | **19** | 0.95 | ✅ `GitHub同步推送-workflow` | 频次刷新 15→19 |
| 2 | 技术突破监控 | **17** | 0.95 | ✅ `daily-tech-breakthrough-monitor` | 频次刷新 13→17 |
| 3 | 负一屏推送（横切） | **31** | 0.90 | ✅ `负一屏推送-workflow` | 频次确认 31，横切于 8 个任务族 |
| 4 | Memory Dreaming Promotion | **11** | 0.90 | ✅ `memory-writer` / `smart-memory` | 不新建（冗余） |
| 5 | 周一综合检查 | **4** | 0.78 | ✅ `周一综合检查-workflow`（上轮新建） | 频次刷新 3→4，验证有效 |
| 6 | **商业智能周报** | **4** | **0.80** | ❌ 无（仅有方法论 skill） | **🆕 新建提案** |
| 7 | **周一知识管理综合** | **3** | **0.75** | ❌ 无 | **🆕 新建提案** |
| 8 | **QClaw 智能清理** | **3** | **0.72** | ❌ 无（仅裸脚本） | **🆕 新建提案** |
| — | Dream 记忆整理 | 2 | — | ✅ `dream-agent` | 不新建（冗余） |
| — | Distill 工作流发现（自身） | 3 | — | ✅ `distill-agent` | 不新建（自指） |

---

## 三、本轮新建的 3 个 skill 提案

### 3.1 `商业智能周报-workflow`（置信度 0.80）
**提案 ID：`workflow-20260803-488b182d67`**

- 命中 4 个日期（07-10 / 07-20 / 07-25 / 07-31），步骤链完全一致
- 关键区分：已有 `business-intelligence` 是**方法论** skill（指标树/KPI 建模），本 skill 固化的是**端到端交付链路**——信号检索 → 决策格式撰写 → `create_task_json.py` → `task_push.py` → 校验 `success:true`。两者是上下游关系，不构成冗余
- 内置沉淀：四轴信号框架（市场资本/技术产品/合规治理/人才组织）、五要素决策格式（What changed / Why / Confidence / Action / Owner）、"交叉印证段落价值最高、纯新闻摘要价值低"的实证教训

### 3.2 `周一知识管理综合-workflow`（置信度 0.75）
**提案 ID：`workflow-20260803-3b1596909a`**

- 命中 3 个日期（07-13 / 07-20 / 08-03），步骤链一致
- **固化的核心价值是避坑清单**——三次执行反复踩同一批坑：
  - gbrain 路径被写成 `C:\Users\Administrator\gbrain`（不存在，且违反规则 6 禁 C 盘）
  - pglite wasm 在 Windows 崩溃导致 GBrain 阻塞
  - cron 场景下 IMA 同步**技术不可达**（无 mediaId），却被当成任务步骤反复"假执行"
  - person 页面 UTF-8/GBK 乱码（与 07-20 乱码事故同源）
  - GBrain 内全是 `john-doe`/`test-person` 测试数据，Tier1 enrichment 空转两个月
- 三次踩同样的坑 = 典型的"知识没被固化"信号，这是本轮最该产出的 skill

### 3.3 `QClaw智能清理-workflow`（置信度 0.72）
**提案 ID：`qclaw-workflow-20260803-4c0e9f67e9`**

- 命中 3 次（07-20 / 08-03 ×2）
- 已有裸脚本 `auto-cleanup-skills.ps1` / `cleanup-skills.ps1`，但无 skill 统一承载，导致每次执行标准不一
- 固化内容：周度/月度分支判定、SHA256 零丢失归档、**Skills 活跃度双条件判定（取代数量上限判定）**、被安全策略拦截操作的正确处置方式（记录上报，禁止绕过）
- 修正了一处历史数据不一致：Skills 计数曾报 333，实测 135（`D:\Users\Administrator\.qclaw\skills`）+ 10（workspace skills）

---

## 四、判定为冗余、不新建的模式

| 模式 | 次数 | 不新建理由 |
|------|------|-----------|
| Memory Dreaming Promotion | 11 | 已由 `memory-writer` + `smart-memory` 承载，cron 每日 11:10 稳定运行 |
| Dream 记忆整理 | 2 | 已由 `dream-agent` 承载，且未达 ≥3 阈值 |
| Distill 工作流发现 | 3 | 本任务自身，由 `distill-agent` 承载 |

遵守"禁止冗余"原则：高频 ≠ 该建 skill，**只有"高频 + 无承载"才固化**。

---

## 五、已有 skill 频次刷新（未改动文件，仅本报告记录）

| Skill | 原记录 | 本轮实测 | 状态 |
|-------|--------|---------|------|
| `GitHub同步推送-workflow` | 15 | **19** | 持续升高，分支动态获取修复仍生效 |
| `daily-tech-breakthrough-monitor` | 13 | **17** | 持续升高 |
| `负一屏推送-workflow` | 31 | **31** | 横切能力，8 个任务族共用 |
| `周一综合检查-workflow` | 3 | **4** | 上轮新建 → 本轮验证有效 |

> 按 Skill Workshop 策略，live skill 的元数据修改需走提案审批。本轮判断"仅刷新数字"价值低于产生 4 条待审提案的噪声成本，故仅在报告中留档；下次如有实质内容变更再一并提案。

---

## 六、本轮最重要的发现

**「三次踩同一个坑」是比「高频重复」更强的固化信号。**

上轮（07-20）判定标准是纯频次（≥3 次即固化）。本轮发现更有价值的指标是**故障复现率**：周一知识管理综合只出现 3 次，频次不算高，但三次全部踩中同一批坑（C 盘路径、wasm 崩溃、IMA 不可达、编码乱码）。这说明该工作流的知识**完全没有沉淀**，每次都在重新踩雷。

建议下轮 Distill 引入第二判定维度：
```
固化优先级 = 重复次数 × 0.5 + 故障复现率 × 0.5
```
纯高频但零故障的工作流（如 GitHub 同步 19 次）说明已经跑顺，固化收益低；低频高故障的（如知识管理 3 次全踩坑）才是真正的知识缺口。

---

## 七、统计信息汇总

| 统计项 | 数值 |
|--------|------|
| 扫描 session 数 | **79** |
| 达到 ≥3 阈值的模式数 | **8** |
| 新创建 skill 提案数 | **3** |
| 已有 skill 频次刷新数 | **4** |
| 判定冗余不固化数 | **3** |
| 提案状态 | 全部 pending，等待用户批准后生效 |

**待批准提案：**
1. `workflow-20260803-488b182d67` — 商业智能周报-workflow
2. `workflow-20260803-3b1596909a` — 周一知识管理综合-workflow
3. `qclaw-workflow-20260803-4c0e9f67e9` — QClaw智能清理-workflow

---

## 八、结论

样本量继续增长（79 vs 65 session），达阈模式从 3 个跃升到 8 个，但其中 5 个已有 skill 承载。真实缺口是 3 个：**商业智能周报、周一知识管理综合、QClaw 智能清理**——都是每周/每月在跑、却从未被固化的定时工作流。

三份提案已生成，处于 pending 状态，**需要你批准后才会写入 live skills**。批准命令形如：让我 apply 对应 proposal_id。

下次建议触发周期：2026-09-01 前后（每月第一个周一）。

**置信度**：🔴高（所有频次均来自 79 个 session 摘要文件的实际扫描与聚类，路径均经 `Test-Path` 校验）
