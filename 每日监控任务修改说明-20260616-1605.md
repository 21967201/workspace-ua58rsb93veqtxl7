# 每日监控任务修改说明 (2026-06-16 16:05)

## 📋 需要修改的任务
`每日监控任务` (Cron任务，每日10:30执行)

## 🔧 修改方法

### 方法1：使用OpenClaw CLI（推荐）
```bash
# 1. 读取当前任务配置
openclaw cron list | findstr "每日监控"

# 2. 获取任务ID
set TASK_ID=...

# 3. 修改任务配置（在task字段开头添加验证脚本）
openclaw cron update %TASK_ID% --task "powershell -Command \"& { & D:\\QClawX\\scripts\\check-data-location.ps1 -HoursAgo 1; # 原有监控逻辑... }\""
```

### 方法2：直接编辑openclaw.json
1. 打开 `D:\QClawX\data\.qclaw\openclaw.json`
2. 找到 `每日监控任务` 配置
3. 在 `task` 字段开头添加：
```powershell
& D:\QClawX\scripts\check-data-location.ps1 -HoursAgo 1; 
```
4. 保存文件
5. 重启OpenClaw服务或等待下次任务执行

## 📝 修改后的task字段示例
```json
"task": "powershell -Command \"& { & D:\\QClawX\\scripts\\check-data-location.ps1 -HoursAgo 1; # 原有监控逻辑... }\""
```

## ✅ 验证方法
修改后，下次 `每日监控任务` 执行时（明日10:30），检查：
1. 任务是否正常执行
2. 是否先运行了 `check-data-location.ps1`
3. 如果检测到违规，是否在推送内容中注明

## 📋 修改优先级
- **P0**: 必须修改（确保规则6被执行）
- **截止时间**: 明日10:30之前

---

**文档创建时间**: 2026-06-16 16:05
**执行人**: QClaw AI Assistant
**下一步**: 使用OpenClaw CLI或编辑openclaw.json完成修改
