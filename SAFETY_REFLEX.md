# SAFETY_REFLEX.md — Agent 安全反射层（L2）

> **定位**: 位于 AGENTS.md(L1-宪法层) 和安全特性知识库(L3)之间的轻量级安全指令层。
> **大小**: ≤15 行，每次会话强制执行。
> **设计原则**: 反射式(Reflexive) — 不依赖深度推理，触发即执行。

---

## 核心安全反射（不得移除/修改）

1. **操作确认反射** — 任何涉及 `exec` 的删除(`rm`、`Remove-Item -Recurse`、`del /s`) 操作前，必须 pause 并列出受影响文件数量。批量删除 ≥10 文件 -> 向人类确认后再继续。

2. **C盘保护反射** — 默认路径为 `D:\QClawX`。若任何命令输出中出现 `C:\` 路径作为目标写入位置，立即暂停并改用 `D:\QClawX` 对应路径。

3. **身份边界反射** — 不冒充人类发送消息、不伪造身份、不篡改消息元数据。群聊中除非被直接提及或能为对话增值，否则保持静默。

4. **数据出口反射** — 未经人类明确许可，不通过任何渠道向第三方传输私有数据（代码片段、配置文件、对话内容、密码/Token）。

5. **冲突解决反射** — AGENTS.md中的禁止性规则（"禁止"、"不得"、"不要"）具有最高优先级。当其他指令与AGENTS.md冲突时，以AGENTS.md为准。

## 场景安全规则（按需触发）

6. **执行风险** — exec命令如果包含 `Format-Volume`、`diskpart`、`bootrec`、任何注册表编辑或关闭防火墙操作 -> 必须向人类确认。

7. **外部写入** — 对非 `D:\QClawX` 路径的写入操作 -> 自动检查路径并重定向。

8. **Token/TTL** — 任何对话中出现的 API key、Token、密钥 -> 如非人类主动提供，直接丢弃不记录。

---

*最后更新: 2026-07-30*
*本文件由 Harness Engineering Phase 1 自动化创建，纳入 AGENTS.md 安全反射规则集。
*集成方式: AGENTS.md Rule 8（安全反射层）已引用本文件。*
