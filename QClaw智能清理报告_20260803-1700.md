# QClaw 智能清理报告（周度 + 月度）

**执行时间**：2026-08-03（周一）17:00 Asia/Shanghai
**任务来源**：cron `QClaw智能清理（周度+月度合并）`
**执行模式**：全自动

---

## 一、目标

按周度轻量清理 + 月度深度分析（本月第一个周一，触发月度分支）清理 QClaw 运行环境，包括临时文件、过期日志、缓存、数据冷热分层归档，并检查 Skills / 内存 / Workspace 体量。

---

## 二、执行结果

### 1. 临时文件清理（周度）
| 项 | 结果 |
|---|---|
| workspace 中 `*.tmp / *.temp / ~*` | **0 个**（本来就干净） |
| 释放空间 | 0 MB |

结论：workspace 无临时文件残留，前几轮清理机制有效。

### 2. 日志清理（周度）
扫描 3 个日志目录，**>7 天的过期日志共 5 个文件，约 0.34 MB**：

| 目录 | 过期文件数 | 大小 |
|---|---|---|
| `D:\QClawX\data\logs` | 1 | ~0 MB |
| `D:\QClawX\data\workspace-ua58rsb93veqtxl7\logs` | 2 | ~0 MB |
| `D:\QClawX\logs` | 2 | 0.34 MB |

⚠️ **状态：未删除**。批量删除被安全策略拦截，需用户在交互界面二次确认后才能执行。体量极小（0.34 MB），不影响系统运行，可下轮合并处理。

### 3. 数据分级归档（冷热分层）✅
执行 `D:\QClawX\scripts\data-tiering.ps1 -Mode run`，SHA256 校验全通过：

- **本轮归档**：成功 6 个文件，失败 0，释放 12.9 KB
- **分层现状**：
  - HOT（热区/工作区根）：357 文件 / 3154.1 KB
  - WARM（温区/未压缩）：573 文件 / 5713.4 KB
    - 2026-05: 47 | 2026-06: 481 | 2026-07: 43 | 2026-08: 2
  - COLD（冷区/已压缩）：0 归档包
- **累计审计**：归档成功 572 | 失败 0 | 恢复 1 | 索引 571 条

📌 **观察点**：COLD 区仍为 0，说明 >90 天的压缩规则尚未触发到实际文件（WARM 区最老为 2026-05，距今约 3 个月，下月起应开始产生冷区归档包）。若下轮仍为 0，需检查 data-tiering.ps1 的 90 天压缩分支逻辑。

### 4. Skills 分析（月度深度）
| 位置 | 数量 |
|---|---|
| `D:\Users\Administrator\.qclaw\skills` | **135** |
| `D:\QClawX\data\.qclaw\skills` | 61 |

- **90 天未活跃的 Skills：0 个**
- 虽然数量 >30 的阈值触发了检查，但**无一个符合"陈旧"判定标准**（全部在 90 天内有文件变动），因此**未执行任何 Skills 删除**。
- 结论：数量多但都"活着"，盲目按数量删除会破坏可用能力。阈值规则本身偏保守，建议改为"90 天无 session 记录 + 无文件变动"双条件，而不是单纯数量上限。

### 5. Agents 分析（月度）
未发现独立的 agents 陈旧目录需清理；当前 agent 工作区活跃。

### 6. Workspace 体量
- 文件数：**16,091**
- 总大小：**402.23 MB**
- 评估：体量正常，无异常膨胀。分层归档已在持续把冷数据搬离热区。

### 7. 内存清理 ✅
| 阶段 | 可用内存 |
|---|---|
| 清理前 | **0.60 GB** / 15.84 GB（严重紧张） |
| GC 回收后 | **2.45 GB** |

释放约 **1.85 GB**。

内存占用 TOP：
| 进程 | MB |
|---|---|
| WorkBuddy | 896.3 |
| node | 857.7 |
| WorkBuddy(2) | 569.4 |
| GameViewer | 430.9 |
| AliRender | 368.9 |
| __ima.copilot | 277.1 |
| QClaw | 266.6 |
| HipsDaemon | 259.0 |

⚠️ **仍低于 4 GB 阈值**。真正的内存大户是 WorkBuddy（双实例合计 1.47 GB）和 node（0.86 GB），非 QClaw 自身。QClaw 只占 266 MB，清理 QClaw 缓存对整机内存帮助有限。

---

## 三、关键结论

1. **系统整体健康**：临时文件为 0，workspace 402 MB 属正常范围，归档链路 572 次零失败。
2. **内存是当前唯一真实瓶颈**：可用内存曾跌到 0.6 GB，GC 后回到 2.45 GB 但仍不足 4 GB。瓶颈来自 WorkBuddy 双实例 + node，**不是清理任务能解决的**，需要在应用层处理（关闭多余 WorkBuddy 实例 / 排查 node 进程归属）。
3. **Skills 清理规则需要改**：135 个 Skills 全部 90 天内活跃，"数量>30 就清理"这条规则在实际数据面前无法执行，也不该执行。建议改成活跃度双条件判定。
4. **冷区（COLD）长期为 0** 值得下轮重点验证，可能是压缩分支未被触发。
5. **日志删除被安全策略拦截**，涉及 5 个过期日志文件共 0.34 MB，需用户二次确认。风险极低但删除不可恢复。

---

## 四、下轮待办

- [ ] 用户确认后清理 5 个 >7 天过期日志（0.34 MB）
- [ ] 验证 data-tiering.ps1 的 90 天 COLD 压缩分支是否正常触发
- [ ] 评估 WorkBuddy 双实例是否可合并/关闭以释放 ~570 MB
- [ ] 修订 Skills 清理规则：数量阈值 → 活跃度双条件（90 天无 session + 无文件变动）
